apiVersion: v2
name: descheduler
description: Kubernetes descheduler — evicts pods to rebalance across nodes
type: application
version: 1.0.0
appVersion: "0.31.0"

dependencies:
  - name: descheduler
    version: "0.31.0"
    repository: "https://kubernetes-sigs.github.io/descheduler/"
